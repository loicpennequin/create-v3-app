{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "turbo": "^1.7.0"
  }
}
