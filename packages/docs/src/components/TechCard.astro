---
import { Sprite } from 'astro-icon';

export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<article>
  <a href="*" class="inner">
    <header>
      <slot name="icon" />
      {title}
    </header>

    <p>
      Lorem ipsum dolor sit amet consectetur adipisicing elit. Voluptatem,
      reiciendis. Voluptas totam quam laboriosam illo aperiam dolores. Quaerat
      vitae veritatis delectus omnis assumenda sit eveniet libero repudiandae
      nam. Voluptas, explicabo.
    </p>

    <footer>Learn more</footer>
  </a>
</article>

<style>
  article {
    padding: var(--size-2);
    border-radius: var(--radius-3);
    background: var(--gradient-24);
    background-size: 25%;
    background-repeat: no-repeat;
    background-position: -50% -50%;
    transition: background-size 0.2s ease-in;
    border: solid 1px var(--border);

    &:hover,
    &:focus-within {
      background-size: 100%;
    }
  }

  article,
  a {
    border-radius: var(--radius-3);
  }
  a {
    background-color: var(--surface-1);
    transition: background-color 0.3s;
    padding: var(--size-3);
    display: flex;
    flex-direction: column;
    gap: var(--size-3);

    &,
    &:visited {
      color: inherit;
    }

    &:hover {
      text-decoration: none;
    }

    &:focus {
      outline: none;
    }

    &:focus footer,
    & footer:hover {
      text-decoration: underline;
    }

    & header {
      display: flex;
      align-items: flex-end;
      gap: var(--size-2);
      font-weight: var(--font-weight-6);
      font-size: var(--font-size-2);
    }

    & footer {
      text-align: right;
    }
  }
</style>
